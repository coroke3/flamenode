# FlameNode Claude Code サブエージェント割当・完全統合版

> **位置づけ (2026-07 更新)**: 2026-05 修正キャンペーンの統合正本。キャンペーン自体は完了済みで、現行の作業ガイドは `AGENTS.md` / `CLAUDE.md`。このファイルは権限・ID・連続枠・公開 API の**仕様根拠を確認する参照資料**として維持する。

作成日: 2026-05-15
対象リポジトリ: `coroke3/flamenode`

このファイルは、Claude Code のメインエージェントとサブエージェントが、この1ファイルだけで FlameNode の大規模修正を実装・レビュー・完了確認できるようにするための自己完結版である。

---

## 0. このファイルの正本性

このファイルは、次の4ファイルが手元になくても実装できるように統合した正本である。

- `flamenode_final_detailed_design.md`
- `flamenode_final_implementation_checklist.md`
- `flamenode_final_consistency_audit.md`
- `flamenode_revision_instructions_answered.md`

実装時は、このファイル、現行コード、README、既存の `設計/` ディレクトリを確認すればよい。現行コードがこのファイルと矛盾する場合、現行コードは修正前の状態として扱い、このファイルを優先する。

本ファイルと機械参照用YAMLの関係:

- 本ファイル: 人間・メインエージェント向けの完全仕様、作業順序、禁止事項、チェックリスト。
- `.claude/flamenode-subagent-routing.yaml`: サブエージェント割当・モデル選択・フェーズ管理の機械参照用サマリ。
- 仕様の詳細が衝突した場合は、本ファイルを優先する。

---

## 1. Claude Code モデル運用ルール

### 1-1. 基本方針

- 基本の実装担当は **Sonnet**。
- 本当に簡単な探索、一覧化、文言候補、テスト結果要約だけ **Haiku**。
- メインエージェント、設計判断、矛盾解消、DB・権限・security・連続枠・公開API漏洩レビューは **Opus**。
- Haiku は節約用。仕様判断、権限判断、DB変更、セキュリティ判断には使わない。
- Sonnet が2回連続で原因特定または修正に失敗したら Opus に上げる。

### 1-2. Haiku に渡してよい作業

- grep、Glob、Read によるファイル探索
- 該当箇所候補の列挙
- 既存ドキュメントの目次化
- 実装対象ファイル候補リストの作成
- UI文言の単純な置換案
- Markdownチェックリスト化
- `npm run typecheck` / `npm run build` / test結果の要約
- 型エラーやlintエラーの機械的な一覧化
- 独立したCSSの軽微な見た目崩れ調査

### 1-3. Haiku に渡してはいけない作業

- DB schema / migration 作成
- Discord ID / X ID / Active X ID の責務判断
- BAN / TOS / CostGuard / 権限判定
- 公開APIの返却項目判断
- スロット予約、解放、分割、結合、ロールバック
- 部番号再計算ロジック
- 既存データ移行
- deprecated項目の扱い判断
- securityレビュー
- 仕様が曖昧な箇所の最終判断

### 1-4. Opus に上げる条件

次のどれかに該当したら、SonnetからOpusへ上げる。

- 現行実装と本ファイルが衝突する。
- DB migration が必要で既存データ破壊の可能性がある。
- 権限を緩める可能性がある。
- 公開APIの返却項目を増やす。
- slot group の整合性が崩れる可能性がある。
- Discord ID、X ID、Active X ID のうち2つ以上が絡む。
- YouTube側unlistedとFlameNode内部unlistedの扱いが絡む。
- 監査ログ、復元、危険操作に関わる。
- サブエージェント同士の結論が食い違う。
- Sonnetが2回修正しても typecheck/build/test が通らない。

---

## 2. FlameNode のプロジェクト前提

FlameNode は、YouTube埋め込みを利用した動画アーカイブ・イベント参加・スロット確保・投稿審査・振り返り上映・第三者イベント開催を扱う Cloudflare ネイティブな動画プラットフォームである。

技術スタック:

- Next.js 15 App Router
- React 19
- TypeScript
- Cloudflare D1 SQLite
- Drizzle ORM
- Cloudflare R2
- Cloudflare KV
- Cloudflare Workers Cron Triggers
- Durable Objects
- Auth.js NextAuth v5
- Discord OAuth
- CSS Modules とグローバルCSS
- Tailwind は使わない

主要ディレクトリ:

- `app/`: Next.js App Router
- `app/(public)/`: 公開エリア
- `app/(auth)/`: 認証エリア
- `app/(admin)/`: 管理エリア
- `app/api/`: Route Handlers / Auth.js / Webhooks
- `src/components/`: React components
- `src/actions/`: Server Actions
- `src/lib/`: DB、認証、外部連携、ユーティリティ
- `src/hooks/`: Hooks
- `src/types/`: 型
- `workers/`: Cloudflare Workers
- `migrations/`: Drizzle migrations
- `設計/`: 設計仕様

---

## 3. 全体方針

### 3-1. FlameNodeの方向性

UIはApple的に整理するが、機能そのものは必要なら残す。**表面はシンプル、機能は深く**する。

- 画面上にすべてを常時表示しない。
- 重要機能は削らない。
- 適切な階層、メニュー、権限画面へ整理する。
- 未実装機能は実装済みのように見せない。
- 将来機能は `設計済み` / `未実装` / `実装済み` を明確化する。
- 実装状況はまずMarkdownでまとめる。

### 3-2. `/admin` と `/manage` の分離

- `/admin` はサイト全体管理者専用。
- `/manage` はイベント運営者・協力者用。
- イベント運営者は担当イベントだけ管理できる。
- 権限があるユーザーだけ管理・運営導線を表示する。
- 権限がない操作は非表示または明確にdisabled。
- 作品の主となるユーザーへ編集権限を要求するボタンは用意してよい。
- `/admin` をイベント運営者向けに広げない。

### 3-3. 上部バー

上部バーは情報を減らすが、テーマ切替とX ID切替は奥に隠さない。

基本要素:

- ロゴ
- 検索
- 投稿
- テーマ切替
- X ID切替
- アカウント

上部バーから外す候補:

- 作品一覧
- イベント
- クリエイター
- おすすめ
- ABOUT

設定、ログアウト、ライブラリ等はアカウントメニューへ寄せる。アクセントカラーはCSS変数やテーマトークンで後から変えやすくする。通常状態バッジは保留だが、情報過多を避ける。

---

## 4. ID設計

### 4-1. Discord ID

Discord IDはログイン・通知・BAN・TOS・admin権限の主体である。作品表示やいいねの主体にはしない。

使用領域:

- Auth.jsログイン
- User
- Session
- BAN
- TOS同意
- 通知
- admin/moderator role
- Discordへの連絡
- `owner_discord_user_id` による所有記録

### 4-2. X ID

X IDは作者・作品・プロフィール・アイコン・いいね/セーブ/ライブラリの主体である。

使用領域:

- 作品作者
- クリエイターページ
- 作品一覧の表示名
- アイコン候補
- いいね
- セーブ
- ライブラリ
- チャプターコメント投稿者
- イベント運営者としての表示

### 4-3. Active X ID

Active X IDは、ログインユーザーが現在どのX IDとして操作しているかを示す。

Active X ID切替で切り替わるもの:

- 自分の作品一覧
- いいね
- セーブ
- ライブラリ
- 全て再生プレイリスト
- アイコン候補
- プロフィール編集対象
- 枠確保時のX ID
- 投稿時の作者X ID
- チャプターコメント投稿者

切り替わらないもの:

- ログイン状態
- BAN
- TOS
- admin role
- 通知先Discordユーザー

### 4-4. 大文字小文字の正規化

X ID等の英数字識別子は、大文字小文字を区別しない前提で扱う。

- 保存・検索・比較の正規化ルールを統一する。
- UI表示は元の表記を維持してよい。
- 判定は正規化後の値で行う。
- 大文字小文字違いで別人扱い、削除不能、重複扱いにならないようにする。

---

## 5. 共通書き込みガード

投稿、編集、枠提出、いいね、セーブ、チャプターコメントなど、全ての書き込み操作は共通ガードを通す。

確認項目:

- ログイン済み
- BANされていない
- TOS同意済み
- CostGuard上、その機能が書き込み可能
- 必要に応じてActive X IDがある
- 必要に応じてActive X IDが承認済み
- 必要に応じて対象データの表示・編集権限がある

未承認X IDで許可されるのは枠確保のみ。

実装要件:

- フロント側で入力不可にするだけでなく、API/Server Action側でも必ず権限チェックする。
- 見た目上は編集できそうだが保存時に弾かれる状態を避ける。
- 見た目は編集不可なのにAPI直叩きで更新できる状態は絶対に残さない。
- イベント運営者は付与された権限に応じて実行可能項目が決まる。
- 作品編集フォームは1ページ内でセクション分割し、権限があるセクションだけ編集可能にする。
- `canEditVideo` の `requiredKey` 省略による広すぎる編集権限を避ける。
- TOS状態は `CurrentUser` または共通Contextで取得できるようにする。

完了条件:

- 権限なしユーザーで全編集系操作を試しても更新できない。
- 権限ありユーザーでは従来通り更新できる。
- API直叩きでも権限なし更新ができない。
- UI上でも編集可能・不可が明確に分かる。

---

## 6. 投稿設計

### 6-1. 枠なし投稿

条件:

- ログイン済み
- BANなし
- TOS同意済み
- CostGuardで投稿可能
- 連携済みActive X IDがある
- Active X IDが承認済み

結果:

- `status = public`
- `scheduling_type = manual`
- `creator_id = Active X ID`
- `owner_discord_user_id = DiscordユーザーID`

禁止:

- `contact_x_id` 自由入力を主体にすること。
- 未連携X IDを自動作成して即公開すること。
- Active X IDなしで投稿すること。

### 6-2. 枠あり投稿

- 未承認X IDでは枠確保のみ可能。
- 作品提出には承認済みActive X IDが必要。
- 作品は原則pending開始。
- 連続枠グループに紐づく場合は、グループ内枠をsubmittedへ更新する。

### 6-3. YouTube ID保存

DBではYouTube動画IDを正とする。

- 入力欄ではURL、短縮URL、IDのいずれも受け付ける。
- 内部では動画IDへ正規化して保存する。
- 表示時に必要ならURLを組み立てる。
- URL形式の違いによる重複や判定漏れを防ぐ。

### 6-4. YouTube unlisted と FlameNode unlisted

YouTube側のunlistedはFlameNodeではpublic相当として扱う。一方、FlameNode内部statusのunlistedは内部限定公開を意味する。この2つを混同しない。

推奨命名:

- `youtube_privacy_status`
- `visibility_status` または `videos.status`

---

## 7. スロット設計

### 7-1. 予約グループ

`reservation_group_id` を使って連続枠をまとめる。

同一グループに含めてよいもの:

- 同じevent
- 同じDiscordユーザー
- 同じX ID
- 時間順に隣接している枠

同一グループに含めてはいけないもの:

- 別イベント
- 別Discordユーザー
- 別X ID
- 時間が重複している枠
- 連続していない枠。ただし中間空き枠を埋めて結合する操作は許可

### 7-2. 予約時

- 連続枠の候補を取得する。
- 全てavailableであることを確認する。
- 時間順に隣接していることを確認する。
- 上限を超えないことを確認する。
- 一部更新失敗時は、更新済み枠を戻す。
- 表示ロジックだけで無理やり結合せず、保存時点で連続性を表現する。

### 7-3. 部分解放

- 端解放: 残りは同一グループ。
- 中央解放: グループを左右に分割。
- 全解放: 全枠availableへ戻す。
- submitted枠は通常解放不可。
- X ID却下時の未提出枠解放は自動ではなく手動。管理画面で影響件数を表示し、運営者が解放・維持を選べるようにする。

### 7-4. 拡張

- 前方隣接枠を追加可能。
- 後方隣接枠を追加可能。
- 中間空き枠を埋めて左右の同一主体グループを結合可能。
- 上限超過は禁止。
- 時間重複は禁止。
- 他ユーザー混在は禁止。

### 7-5. 表示

- 連続取得した枠は1つのまとまりとして表示する。
- `10:00〜10:06 / 10:06〜10:12` ではなく `10:00〜10:12` のように表示する。
- ダッシュボード、一覧、詳細画面の表示を一致させる。
- 管理画面ではグループ単位表示に加え、個別枠も確認できるようにする。

---

## 8. 部番号設計

### 8-1. ソート

基本ソートは以下の優先順に統一する。

1. `start_time` あり
2. `start_time` 昇順
3. `end_time` 昇順
4. `sort_order` 昇順
5. `id` 昇順

ユーザー向け画面では時系列以外の意味不明な並びを出さない。

### 8-2. 新しい部にする条件

- 前枠終了から次枠開始までの差が、イベント設定の部区切り時間以上。
- 日付が変わる。
- `slot_kind` が変わる。
- 明示的な区切りがある。
- 時間なし枠へ移る。

### 8-3. 部区切り時間

- イベントごとに設定可能。
- デフォルト30分。
- 未設定・異常値は30分にフォールバック。
- 管理画面で編集可能。
- 変更後は再計算。

### 8-4. 再計算

- 一括生成した枠や後から追加した枠を含め、時系列に応じて部番号を再計算する。
- 後から前方の時間帯を追加した場合、新しい最前部を1部にし、それ以降を2部、3部へ繰り下げる。
- 時間なし枠は、どの部に属するのか、独立枠なのか、ソート順を明確化する。
- 管理画面で算出根拠を確認できるようにする。

---

## 9. いいね・セーブ・ライブラリ・再生リスト

これらはDiscord ID主体ではなく、X ID主体で管理する。

必須要件:

- `videoInteractions.x_user_id` などX ID主体の構造を維持・強化する。
- Active X ID切替で、いいね・セーブ・ライブラリ・再生リストが切り替わる。
- 全て再生時に出るべきプレイリストが出ない不具合を修正する。
- UIだけでなく取得API、クエリ、playlist生成も確認する。
- いいね/セーブの保存先IDを開発環境で確認できるようにする。
- 未承認X IDでの操作制限、TOS、BAN、CostGuardの共通判定を追加する。

完了条件:

- アクティブX ID切替で、いいね・セーブ・ライブラリ・再生リストがすべて切り替わる。

---

## 10. X ID・プロフィール・アイコン

### 10-1. X IDベースのアイコン表示

作品・プロフィール・一覧のアイコンはX IDベースで統一する。

- X IDから取得可能なアイコンを画面上に表示する。
- 作品やプロフィールに表示するアイコンはActive X IDに連動させる。
- 一覧、詳細、ダッシュボードなど主要画面で一貫したアイコン解決を使う。

### 10-2. アイコン選択・アップロード

プロフィール設定に、既存候補から選ぶ・新規アップロードする、の2択を用意する。

- 候補一覧はURL文字列ではなくプレビュー付き一覧で出す。
- 候補は自分のX IDや自分の作品に紐づくアイコンを元に出す。
- アップロード時は250×250程度の正方形を基本にする。
- 無制限アップロード形式にはしない。
- 保存後、一覧・詳細・ダッシュボードへ反映する。

### 10-3. X ID直接編集の廃止または制限

- 作品投稿・編集時の提出主体X IDは自由入力ではなく候補選択を基本にする。
- 枠確保のみDiscordログインで可能にするが、X ID申請は必要。
- 枠確保時点では承認済みかどうかを問わない範囲を設ける。
- 投稿やチャプターコメントではX ID申請・承認状態をチェックする。
- 不正なID変更で編集権限や紐付けが壊れないようにする。

### 10-4. X ID却下・統合・旧データ

- X ID却下時の未提出枠解放は自動ではなく手動運用。
- X ID却下と作品の扱いは状態を明示し、必要な操作制限をかける。
- X ID連携解除は権限が壊れないようにする。
- 旧データ由来X IDはフロント上のユーザー表示では通常X IDと大きく分けない。
- 本人編集権限はDiscord連携・承認状態に基づいて判定する。
- X ID統合・別名管理は後回しにしすぎず、必要な範囲はMVP段階で扱えるようにする。

---

## 11. 候補表示・一覧取得

検索候補、合作メンバー候補、登録ユーザー候補などが不足しないようにする。

- 固定件数だけで終わらせない。
- ページネーション、無限スクロール、追加読み込み、検索を組み合わせる。
- 入力途中で候補が消える状態を避ける。
- 候補がない、まだ読み込んでいない、入力形式が違う、を区別して表示する。
- 昔のユーザー、合作メンバーも候補表示する。
- 2ページ目以降が必要な場合は遷移・追加取得できる。

---

## 12. ユーザー詳細ページ

Discord軸とX軸を整理する。

- UI上は1ページでもよいが、どの情報がどのIDに属するか明確にする。
- Discord主体の情報、X主体の情報、両者の紐付け表示を分ける。
- 誰のDiscordと誰のX IDが紐づいているか確認できるようにする。
- X IDページでは作品・プロフィール・アイコン・活動名をX ID主体で表示する。
- Discordページではログイン・連携・権限・管理情報を中心にする。

---

## 13. チャプターコメント設計

### 13-1. データ

チャプターコメントは最小構成でよい。

- `id`
- `video_id`
- `x_user_id`
- `chapter_time`
- `chapter_label`
- `note`
- `visibility`
- `show_on_player_bar`
- `created_at`
- `updated_at`

### 13-2. 投稿条件

- ログイン済み
- BANなし
- TOS同意済み
- CostGuardで投稿可能
- Active X IDあり
- Active X ID承認済み
- 対象動画がFlameNode内publicまたはunlisted

YouTube側unlistedでFlameNode内publicなら投稿可。

### 13-3. 表示

- publicは誰でも表示。
- privateは投稿者本人、作品作者、admin、許可運営者のみ。
- スマホでは動画下に表示。
- PCでは右レールまたは動画横に表示。

### 13-4. 禁止

- 独立コメント欄を作らない。
- `video_comments` を新規利用しない。
- コメントは必ず時刻に紐づける。
- 返信・スレッドを作らない。
- 未承認X IDでは投稿不可。
- public/unlisted以外には投稿不可。
- `marker_kind` に依存した分岐を増やさない。

---

## 14. 公開API設計

### 14-1. 返却可

- public作品
- YouTube側unlistedだがFlameNode内public作品
- publicイベント
- publicクリエイター
- publicチャプターコメント
- publicメンバー情報
- 残り枠など公開してよい集計

### 14-2. 返却不可

- Discord ID
- email
- role
- is_banned
- TOS状態
- active_x_user_id
- linked_discord_user_id
- verification_token
- internal_note
- private note
- void_detail_private
- notification payload
- access_token
- refresh_token
- 管理者向け履歴

### 14-3. パフォーマンス

- limit上限
- page / offset
- キャッシュ
- 検索負荷対策
- 将来的な静的JSON化

公開APIは返却項目をホワイトリスト化する。

---

## 15. health / security

healthはデータ整合性。securityは漏洩・権限・危険状態を検出する。

### 15-1. healthチェック項目

- system_settingsがglobal 1行
- primary_event_idとvideo_eventsが同期
- 存在しないevent/video参照がない
- available slotにvideo_idがない
- submitted slotにvideo_idがある
- slot時間重複がない
- reservation_group_id内に別ユーザー混在がない
- public動画にyoutube_video_idがある
- voided動画が公開されない
- like_countと実数が大きくズレていない
- deprecated項目に新規データが増えていない

### 15-2. securityチェック項目

- accounts.access_tokenがnull
- rejected X IDがactiveでない
- 未承認X IDで投稿済み作品がない
- BANユーザーの書き込みがない
- TOS未同意ユーザーの書き込みがない
- 公開APIに内部情報が出ていない
- custom pageで危険HTMLが無効化される
- 管理操作がhistory_logsに残る

### 15-3. 管理画面

- DB整合性チェックページを作る。
- セキュリティ点検ページを作る。
- OAuth access_tokenが保存されていないか検査する。
- 管理者、モデレーター、イベント運営者を一覧確認できるようにする。
- 公開状態、スロット状態、イベント所属などの矛盾を検出する。
- 検出した問題はDry Run後に修復できるようにする。

---

## 16. UI / UX修正

### 16-1. トップページ

トップページに最低限以下を表示する。

- 開催日時
- 募集期間
- 残り枠
- イベント概要
- 参加方法の概要

トップページを見ただけで、いつ何が行われ、あとどれくらい空きがあるか分かるようにする。

### 16-2. スロット確保導線

- `スロット確保する` ボタンは直接スロット確保画面へ遷移させる。
- ボタン名と遷移先を一致させる。
- エントリー画面では `イベントに参加する` / `過去の自分の作品を投稿する` の2択を中心にする。

### 16-3. ダッシュボード

- 上部バーはグローバル導線。
- ダッシュボードは概況、状態、次の操作を見せる場所。
- 同じ遷移先が2系統あるだけの状態を解消する。
- 違うなら違いが分かるラベル・配置にする。

### 16-4. 編集可能感

- 編集可能箇所にはホバー、枠線、プレースホルダ、編集アイコンなどを使う。
- 編集不可箇所はdisabled風にする。
- 権限がないが申請可能な場合は権限要求導線を出してよい。

### 16-5. 文言改善

- `レコメンドのスコアから見る` は `おすすめを見る` などに変更する。
- 管理画面向けの硬い用語を公開UIに出さない。
- 意味が曖昧にならないようにする。

### 16-6. 行高・配置崩れ

- 横ベタ並びを避け、グリッド・折返し・カード化を使う。
- PC幅だけでなく狭い横幅でも破綻しないようにする。
- 要素数が増えても横にベタ並びで壊れないようにする。
- 管理テーブルはカード、フィルタ、要対応表示と組み合わせる。
- モバイル管理も最低限操作できるようにする。

---

## 17. 入力UI改善

### 17-1. 編集ソフト欄

- 既存データを元に候補を作る。
- 選択式を基本にする。
- その他が必要なら自由入力の補助欄を追加する。
- 表記揺れは後から置換・統合できるようにする。

### 17-2. クレジット入力

- 入力例を表示する。
- 途中入力でも形式が分かるようにする。
- 可能なら候補補完を用意する。

### 17-3. 時刻入力

以下のいずれか、または組み合わせを導入する。

- 時刻ピッカー
- 候補ボタン
- 連続追加前提の簡易UI
- 一括入力補助

CSVインポートは任意。まず通常操作を楽にする。

---

## 18. データ設計

### 18-1. イベント所属と主イベント

- 複数イベント所属欄に所属する全イベントを保存する。
- その中から1つ、枠・上映上の所属イベントを主イベントとして扱う。
- 主イベントは所属イベントの中から1つ選ぶ。
- 主イベントは枠・上映・代表表示に使う。
- 主イベントが所属イベント一覧に含まれていない状態を禁止する。
- `/admin/health` で不整合を検出する。

### 18-2. DB削除・整理

- `video_comments` はdeprecated扱い。
- チャプター種別カラムはMVPでは固定値または削除候補。
- `outro_comment` と `closing_comment` は統一する。
- X ID再申請情報・無効化情報は当面作品本体に保持してよい。
- スロット表示名は、作品提出後は作品情報の活動名に同期する。
- 提出前は仮名を保持してよい。
- 自由HTML/CSS機能はMVPから外さず、独自タグ・ショートコード・サニタイズを前提に安全設計する。
- APIキー管理は作る。スコープ、レート制限、有効期限を持たせる。
- 使用ソフト辞書は既存データを元に選択式にする。
- コスト使用量履歴は残すが、履歴保存自体が無料枠を消費しすぎないようにする。
- ダッシュボード集計キャッシュは、更新経路がある場合のみ使う。
- `system_settings` はglobal 1行に統一する。
- 削除は論理削除・アーカイブ優先。
- 監査ログが必要操作で残る。

---

## 19. 通知・運営フロー

通知は各処理から直接送るのではなく、通知キューに積む。

通知対象:

- X ID申請
- X ID承認/却下
- 作品提出
- 作品承認/却下
- 修正依頼
- 規約再同意
- コストガード変更

要件:

- Discord通知だけでなくサイト内通知も用意する。
- サイト内通知はDiscordと同等内容。
- 通知失敗時は再試行・失敗履歴を管理画面で見られるようにする。
- イベント運営者向け受信箱を作る。

---

## 20. Worker・静的JSON・運用

実装済み・未実装・設計のみを明確にする。

対象:

- 静的JSON生成
- YouTube同期
- スコア再計算
- 期限切れ処理
- 通知Dispatcher

方針:

- まずMarkdownで実装状況をまとめる。
- 実装済みのものだけデプロイ手順に載せる。
- 未実装のものは設計メモとして分離する。

---

## 21. インポート・旧データ

旧データ取り込みは管理者専用の危険操作として扱い、Dry Runを必須にする。

- 実行前に必ずDry Runで作成・更新・スキップ・エラー件数を表示する。
- ファイルサイズ・件数制限を設ける。
- 旧データ由来X IDは表示・作品紐付け上は通常X IDと大きく分けない。
- 本人編集権限はDiscord連携・承認状態に基づいて安全に判定する。
- 差分表示を行う。

---

## 22. 危険操作・監査ログ

危険操作には共通モーダルを使い、影響件数を表示する。

- 削除、無効化、却下、一括操作は危険操作として扱う。
- 影響する作品・枠・ユーザー数を表示する。
- 必要に応じて確認文字列を入力させる。
- 重要操作はすべて監査ログに残す。
- 監査ログはJSONそのままではなく変更前後の差分として表示する。
- 物理削除は要再検討。
- historyに差分を保存して復元できる設計を前提にする。
- 復元機能が未実装の間は、イベント・作品・ユーザー関連データの破壊的削除を制限する。

---

## 23. 管理画面・運営画面UX

管理トップは `今日やること` を最初に表示する。

- 審査待ち作品、X ID申請、修正依頼、期限切れ候補をキュー化する。
- イベント管理画面には、基本情報、受付、枠、権限、公開プレビューの準備チェックを出す。
- 作品・イベント・X ID詳細では、編集フォームより先に現在の状態カードを出す。
- 管理画面はテーブルだけでなく、カード、フィルタ、要対応表示を組み合わせる。
- モバイル管理も最低限操作できるようにする。

---

## 24. 作業順序

以下の順で作業すること。

1. IDと権限の整理
2. 連続枠・部ロジックの再設計
3. いいね・セーブ・ライブラリの紐付け修正
4. 一覧取得件数・候補表示修正
5. アイコン選択・アップロード整備
6. UI導線・文言・見た目修正
7. DB整理・未使用機能の明確化
8. 通知・運営フロー整備
9. 整合性チェック・セキュリティチェック整備

個別にその場しのぎで直さない。まず責務整理を行ってから画面修正に入る。

---

## 25. 開発時デバッグログ

開発環境では、重要操作時に以下を確認できるようにする。

- 現在のアクティブX ID
- 現在の表示主体ID
- いいね/セーブの保存先ID
- 枠グループID
- 部番号の算出根拠
- 権限判定結果
- 投稿・編集・チャプターコメントの共通ガード判定結果

本番では不要な内部ログを出しすぎない。

---

## 26. テスト観点

### 26-1. ID切替

- X ID切替で作品一覧が変わる。
- X ID切替でいいね/セーブが変わる。
- X ID切替でライブラリ・再生リストが変わる。
- X ID切替でアイコン候補が変わる。

### 26-2. 権限

- 権限なしでは編集不可。
- 権限ありでは編集可能。
- API直叩きでも権限なし更新不可。
- 権限があるセクションだけ編集できる。

### 26-3. 枠管理

- 単独枠確保。
- 連続枠確保。
- 連続枠解放。
- 中央部分解放。
- 隣接拡張。
- 中間空き枠を埋めた結合。
- 一括生成後の前追加・後追加。
- 部番号再計算。
- 時系列ソート。
- 提出済み枠の誤解放防止。

### 26-4. UI

- 編集可能感が分かる。
- 入力形式が分かる。
- 候補不足にならない。
- スロット確保導線が自然。
- トップページで必要情報が分かる。
- 上部バーが情報過多にならない。
- テーマ切替とX ID切替が使いやすい位置にある。
- 狭い幅でも崩れない。

### 26-5. コメント/チャプター

- 独立コメント欄が存在しない。
- チャプターコメントが時刻に紐づく。
- チャプターコメントは権限に応じて編集・削除できる。
- スマホで動画下にチャプターコメントが出る。

### 26-6. 公開API/security

- publicな作品だけ返す。
- YouTube側unlistedでFlameNode内publicの作品は返す。
- Discord IDを返していない。
- emailやroleを返していない。
- private/internal noteを返していない。
- ページネーションがある。
- limit上限がある。
- 公開APIに内部情報が出ていない。

---

## 27. 要再検討・保留事項

以下は完全確定ではないため、実装前に最終確認する。ただし実装を止めず、矛盾が出る箇所だけOpus判断に上げる。

- アカウントメニュー統合: 設定・ログアウト等はメニューへ寄せるが、テーマ切替とX ID切替は上部からすぐ触れる導線を残す。
- 通常状態バッジ: 消すかどうかは保留。必要な画面では表示を許可。
- イベント編集者と細かい権限テーブル: 既存の考え方を崩さず、矛盾が出る箇所だけ整理。
- 物理削除: 復元可能性を重視し、復元機能未実装の間は危険操作を制限。

---

## 28. 回答がNOだった項目の代替方針

- FlameNodeの方向性: 機能を削ってまで単純化しない。UIは整理し、必要機能は残す。
- MVP範囲: 将来機能を機械的に削らない。未実装機能は未実装と明示。
- 細かい編集権限: イベント運営者の編集可否は付与された権限に応じて決める。
- テーマ切替: アカウントメニューの奥だけに隠さない。
- X ID表示: X IDスイッチャーは奥だけに隠さない。
- 枠なし投稿: 一律審査待ちにしない。条件を満たせば即公開。
- X ID却下: 未提出枠解放は自動ではなく手動。
- X ID統合: 後回しにしすぎず、必要な範囲はMVPで扱えるようにする。
- メンバー表: 初期状態で隠しすぎない。
- 関連動画: 作品回遊に役立つ位置へ表示。
- X ID再申請中作品: 一覧・検索から一律排除しない。状態を明示して制限。
- イベント所属: 複数イベント所属を正とし、主イベントをその中から選ぶ。
- X ID再申請情報: 当面作品本体に保持してよい。
- 無効化情報: 当面作品本体に保持してよい。
- スロット表示名: 作品提出後は作品情報の活動名へ同期。提出前は仮名。
- 自由HTML: MVPから外さない。独自タグ・ショートコード・サニタイズで安全に設計。
- プロフィールページ: 構造化ブロックだけに限定しない。
- APIキー管理: 作る。スコープ、レート制限、有効期限を含める。
- 使用ソフト辞書: 既存データを元に選択式へ寄せる。
- コスト使用量履歴: 残すが、無料枠消費とバランスを取る。
- 実装状況ページ: 管理画面より先にMarkdownを作る。
- 旧X ID: 表示・作品紐付け上は通常X IDと大きく分けない。

---

## 29. GitHub現行実装との注意点

### 29-1. ID・権限

現行実装では `CurrentUser` に `active_x_user_id` と `is_banned` はあるが、TOS状態がない可能性がある。TOS再同意を投稿・編集・チャプターコメントに効かせるには、共通ガードでTOS状態を取得できるようにする。

現行の `canEditVideo` は `requiredKey` を省略するとイベント権限を持つユーザーが広く編集できる可能性がある。セクション単位で`requiredKey`を明示する。

### 29-2. スロット

現行実装には `reservation_group_id` があり、連続枠の基礎はある。ただし、途中失敗時のロールバック、部分解放、グループ分割、拡張、結合が不足している可能性がある。

### 29-3. 枠なし投稿

現行実装は `contact_x_id` をフォームから受け、必要ならX IDを作る構造になっている可能性がある。枠なし投稿は連携済みActive X ID必須であり、自由入力X IDを投稿主体にしない。

### 29-4. いいね / セーブ

現行実装で `videoInteractions.x_user_id` を使っている場合はX ID主体方針と一致。ただし、未承認X IDでの操作制限、TOS、BAN、CostGuardの共通判定を追加する。

### 29-5. チャプター

現行実装で `videoChapters` を使い、Active X IDかつ承認済みを要求しているなら方向性は一致。ただし、対象動画状態、BAN、TOS、CostGuardチェックを追加する。`marker_kind` はMVPでは実質不要なので固定値扱いにする。

### 29-6. 公開API

現行の `/api/videos` が公開作品のみ返しているなら基本方針と合っている。今後拡張する際は、返却項目のホワイトリスト化、内部情報除外、ページネーション、キャッシュを守る。

### 29-7. admin

現行の `/admin` はadmin専用であり、今回の イベント運営者は `/manage` 方針と矛盾しない。`/admin` を広げず、`/manage` を新設する。

---

## 30. 実装後に矛盾が起きやすいポイント

- YouTube unlisted と FlameNode unlisted の混同。
- 枠なし投稿のX IDを自由入力に戻すこと。
- イベント運営者の編集範囲を `requiredKey` なしで広げること。
- 連続枠の一部更新後にグループ情報を再分割・再結合しないこと。
- `video_comments` や `marker_kind` を便利だから再利用すること。
- deprecated DB項目へ新規書き込みすること。
- UIだけ直してサーバー側権限チェックを放置すること。

---

## 31. 全サブエージェント共通禁止事項

- 権限をフロントだけで制御し、サーバー側でチェックしないこと。
- 見た目は編集不可なのにAPI直叩きで更新できる状態を残すこと。
- Discord IDとX IDを混同して、表示・編集・ライブラリの主体を曖昧にすること。
- `owner_discord_user_id` だけで編集許可すること。
- Active X IDなしで投稿・いいね・セーブ・ライブラリ・チャプターコメントを書き込ませること。
- 未承認X IDで作品投稿やチャプターコメント投稿を許可すること。
- 未承認X IDで枠確保以外を許可すること。
- `contact_x_id` 自由入力を投稿主体として扱うこと。
- 未連携X IDを自動作成して即公開すること。
- 連続枠を画面上だけまとめ、DB上ではバラバラのままにすること。
- 独立コメント欄を新設・復活すること。
- `video_comments` を新規利用すること。
- `marker_kind` 依存の分岐を増やすこと。
- 未実装機能を実装済みのようにUIやデプロイ手順へ載せること。
- X IDやYouTube URLを正規化せず保存・比較すること。
- 危険操作の影響件数を出さずに実行できるようにすること。
- 上部バーに全ナビゲーションを詰め込むこと。
- テーマ切替とX ID切替を使いづらい奥に隠すこと。
- 公開APIでDiscord ID、email、role、BAN状態、TOS状態、token、private noteを返すこと。

---

## 32. PR分割推奨

大規模修正は次のように分ける。

1. `docs/implementation-plan`
   - 実装状況Markdown、作業順序、チェックリスト更新
2. `auth/id-write-guard`
   - Discord/X/Active X ID、TOS/BAN/CostGuard、共通ガード
3. `posting/youtube-id-and-active-x`
   - 枠なし投稿、枠あり提出、YouTube ID正規化
4. `slots/reservation-groups`
   - 連続枠、部分解放、拡張、結合
5. `slots/part-numbering`
   - 部番号、休憩閾値、管理画面設定
6. `interactions/x-id-library`
   - いいね、セーブ、ライブラリ、再生リスト
7. `comments/chapter-comments-only`
   - 独立コメント廃止、チャプターコメント統合
8. `api/public-whitelist`
   - 公開APIの返却項目整理、ページネーション、キャッシュ
9. `admin/health-security`
   - health/securityチェック、Dry Run、監査ログ
10. `ui/navigation-entry-forms`
    - 上部バー、エントリー、トップ、入力UI、文言

特に `auth/id-write-guard` と `slots/reservation-groups` は他PRと混ぜない。

---

## 33. 作業フェーズとサブエージェントプロンプト

### Phase 0: 現状把握・差分地図作成

推奨モデル: Haiku または Sonnet。

```text
FlameNode の現行コードから、ID/権限、投稿、スロット、部番号、いいね/セーブ/ライブラリ、チャプターコメント、公開API、health/security に関係するファイルを探してください。

出力は以下の形式にしてください。
1. 領域名
2. 関連ファイル
3. そのファイルが担っていそうな責務
4. このファイルとのズレの可能性
5. Haikuで触ってよいか、Sonnet以上が必要か

コード変更はしないでください。
```

### Phase 1: ID・権限・共通書き込みガード

推奨モデル: Sonnet。境界判断はOpus。

```text
FlameNode の Discord ID / X ID / Active X ID / TOS / BAN / CostGuard / 承認済みX ID の責務を整理し、全書き込み操作で使う共通ガードを設計・実装してください。

必ず守ること:
- Discord ID はログイン、BAN、TOS、通知、admin/moderator role の主体。
- X ID は作者、作品、プロフィール、アイコン、いいね、セーブ、ライブラリ、チャプターコメント投稿者の主体。
- Active X ID は現在どのX IDとして操作しているかを示す。
- 未承認X IDで許可するのは枠確保のみ。
- 投稿、編集、いいね、セーブ、ライブラリ、チャプターコメントは共通ガードを通す。
- フロントだけでなくサーバー側で必ず拒否する。
- owner_discord_user_idだけで編集許可しない。

出力:
1. 既存コードの問題点
2. 共通ガードの責務
3. ガード関数/APIの候補名
4. 呼び出し元一覧
5. 実装内容
6. テスト観点
```

### Phase 2: 投稿フロー・YouTube ID正規化

推奨モデル: Sonnet。

```text
FlameNode の投稿フローを修正してください。

必須方針:
- 枠なし投稿は、ログイン済み・BANなし・TOS同意済み・CostGuard許可・連携済みActive X IDあり・承認済みX IDなら即 public。
- 枠なし投稿で contact_x_id 自由入力を投稿主体にしない。
- 未連携X IDを自動作成して即公開しない。
- 枠あり提出は承認済みActive X ID必須。
- 未承認X IDでは枠確保のみ可能。
- YouTube URL / 短縮URL / ID を受け取っても、DBには YouTube動画ID として保存する。
- YouTube側unlistedでも FlameNode内public なら通常公開扱い。

出力:
1. 変更した投稿経路
2. YouTube ID正規化関数の所在
3. contact_x_id自由入力を無効化した箇所
4. テストケース
5. 互換性リスク
```

### Phase 3: 連続枠・部分解放・拡張・部番号

推奨モデル: Sonnet。reservation_group_id再設計やmigrationはOpus。

```text
FlameNode の連続枠ロジックと部番号を修正してください。

必須方針:
- reservation_group_id で連続枠をまとめる。
- 同一グループには同じevent、同じDiscordユーザー、同じX IDの枠だけを含める。
- 連続予約時は全枠available、時間順に隣接、上限内であることを確認する。
- 一部更新失敗時は更新済み枠を戻す。
- 端解放では残りは同一グループ。
- 中央解放では左右にグループ分割。
- 全解放ではavailableへ戻す。
- submitted枠は通常解放不可。
- 前方/後方隣接枠の追加、中間空き枠を埋めた結合を許可する。
- 上限超過、時間重複、他ユーザー混在は禁止。
- 部番号は時系列と連続性から導出し、固定ラベル化しない。
- 部区切り時間はイベントごとに設定可能、デフォルト30分、異常値は30分。

出力:
1. 既存実装とのズレ
2. 必要な関数一覧
3. 予約・解放・拡張・結合の実装内容
4. 失敗時ロールバック方針
5. 部番号算出ロジック
6. テストケース
```

### Phase 4: いいね・セーブ・ライブラリ・再生リスト

推奨モデル: Sonnet。

```text
FlameNode のいいね・セーブ・ライブラリ・再生リストを X ID 主体に統一してください。

必須方針:
- Discord ID主体にしない。
- Active X ID切替で内容が切り替わる。
- いいね/セーブの保存先IDを開発環境で確認できるようにする。
- 共通書き込みガードを通す。
- 未承認X IDでの操作制限、TOS、BAN、CostGuardを確認する。

出力:
1. 取得クエリの変更箇所
2. 書き込み処理の変更箇所
3. X ID切替時の再取得方法
4. 再生リスト生成の修正内容
5. テストケース
```

### Phase 5: チャプターコメント統合

推奨モデル: Sonnet。

```text
FlameNode のコメント機能をチャプターコメントへ統合してください。

必須方針:
- 独立コメント欄を作らない。
- video_comments を新規UI/APIで使わない。
- コメントは必ず時刻に紐づく。
- note を本文として扱う。
- 返信・スレッドは作らない。
- Active X IDあり、承認済み、BANなし、TOS同意済み、CostGuard許可を要求する。
- 対象動画が FlameNode 内 public または unlisted の場合のみ投稿可能。
- YouTube側unlistedで FlameNode内public なら投稿可能。
- private は投稿者本人、作品作者、admin、許可運営者のみ表示。
- スマホでは動画下に表示する。
- marker_kind に依存した分岐を増やさない。

出力:
1. 廃止・非使用にした独立コメント経路
2. チャプターコメント投稿API/Action
3. 表示権限ロジック
4. モバイル表示調整
5. テストケース
```

### Phase 6: 公開API・health・security

推奨モデル: Sonnet。漏洩レビューはOpus。

```text
FlameNode の公開API、health、securityチェックをレビュー・修正してください。

公開APIで返却可:
- public作品
- YouTube側unlistedだが FlameNode内public作品
- publicイベント
- publicクリエイター
- publicチャプターコメント
- publicメンバー情報
- 残り枠など公開してよい集計

公開APIで返却不可:
- Discord ID
- email
- role
- is_banned
- TOS状態
- active_x_user_id
- linked_discord_user_id
- verification_token
- internal_note
- private note
- void_detail_private
- notification payload
- access_token
- refresh_token
- 管理者向け履歴

health/securityは本ファイルのチェック項目をすべて確認できるようにしてください。

出力:
1. APIごとの返却項目一覧
2. ホワイトリスト化した箇所
3. health/securityチェック一覧
4. 漏洩リスク
5. テストケース
```

### Phase 7: UI/UX・入力UI・文言

推奨モデル: Sonnet。文言候補だけならHaiku可。

```text
FlameNode の UI/UX を修正してください。

必須方針:
- 上部バーはロゴ・検索・投稿・テーマ切替・X ID切替・アカウントを中心に整理する。
- 作品一覧、イベント、クリエイター、おすすめ、ABOUT等の常時ナビは上部バーから外す。
- テーマ切替とX ID切替は奥に隠しすぎない。
- トップページに開催日時、募集期間、残り枠、イベント概要、参加方法を表示する。
- スロット確保する は直接スロット確保画面へ飛ばす。
- エントリーページは イベントに参加する / 過去の自分の作品を投稿する の2択中心にする。
- 編集可能箇所と編集不可箇所を見た目で分ける。
- 文言は自然にする。
- 行高・配置崩れを直す。
- 狭い幅でも崩れないようにする。
- 編集ソフト欄は既存データを元に選択式へ寄せる。
- クレジット欄は入力例・候補・プレースホルダを出す。
- 時刻入力はピッカー、候補ボタン、連続追加UI、一括入力補助などで楽にする。

出力:
1. 変更した画面
2. 変更した導線
3. 編集可能/不可の見せ方
4. モバイル確認結果
5. 残ったUI課題
```

### Phase 8: DB整理・deprecated項目・旧データ

推奨モデル: Sonnet。migrationと破壊的変更はOpus。

```text
FlameNode のDB整理・旧データ取り込み安全設計を進めてください。

必須方針:
- deprecated項目へ新規書き込みしない。
- video_comments を使わない。
- outro_comment を新規利用しない。
- primary_event_id と video_events を同期する。
- system_settings は global 1行。
- 削除は論理削除・アーカイブ優先。
- 監査ログが必要操作で残る。
- 旧データ取り込みは管理者専用の危険操作として扱う。
- Dry Runで作成・更新・スキップ・エラー件数を表示する。
- ファイルサイズ・件数制限を設ける。
- 旧データ由来X IDは表示上通常X IDと大きく分けない。
- 本人編集権限はDiscord連携・承認状態に基づく。

出力:
1. DB項目の分類表
2. 新規書き込み禁止にした項目
3. migrationの有無
4. Dry Run表示内容
5. 監査ログ対象
6. Opusレビューが必要な箇所
```

### Phase 9: テスト・ビルド・最終レビュー

推奨モデル: Haikuは結果要約のみ。修正はSonnet。最終レビューはOpus推奨。

```text
FlameNode の変更後チェックを実行し、結果を要約してください。

最低限確認:
- npm run typecheck
- npm run build
- DB migration がローカルで通るか
- 権限なしユーザーでAPI直叩きテスト
- X ID切替テスト
- 連続枠の部分解放テスト
- 連続枠の拡張テスト
- 部番号再計算テスト
- チャプターコメント投稿テスト
- 公開APIの漏洩チェック
- health/securityチェック

Haikuの場合は結果の要約まで。失敗修正はSonnet以上に渡してください。
```

---

## 34. 実装チェックリスト

### 34-1. PR基本

- [ ] 目的が明確
- [ ] 関連Issueがある
- [ ] DB変更の有無が明記されている
- [ ] 権限変更の有無が明記されている
- [ ] UI変更の有無が明記されている
- [ ] 破壊的変更の有無が明記されている

### 34-2. ID / 権限

- [ ] Discord IDとX IDの責務が混ざっていない
- [ ] Active X ID切替で対象データが切り替わる
- [ ] いいね/セーブ/ライブラリがX ID単位
- [ ] owner_discord_user_idだけで編集許可していない
- [ ] 未承認X IDは枠確保のみ
- [ ] BAN/TOS/CostGuardが書き込み操作に効く
- [ ] UI disabledだけでなくサーバー側でも拒否している
- [ ] CurrentUserまたは共通ContextでTOS状態を見られる

### 34-3. 投稿

- [ ] 枠なし投稿は連携済みActive X ID必須
- [ ] 枠なし投稿は承認済みX ID必須
- [ ] 枠なし投稿は条件を満たせば即public
- [ ] contact_x_id自由入力で即公開できない
- [ ] 未連携X IDを自動作成して即公開しない
- [ ] 枠あり提出は承認済みActive X ID必須
- [ ] 未承認X IDでは枠確保のみ可能
- [ ] YouTube IDはURLではなくIDとして保存される
- [ ] YouTube側unlistedでもFlameNode内publicなら通常公開扱い

### 34-4. 編集フォーム

- [ ] セクションごとに編集可否が分かれる
- [ ] 権限がないセクションは見た目で分かる
- [ ] フィールド単位でサーバー側権限チェックがある
- [ ] YouTube ID変更は重複チェックがある
- [ ] X ID自由入力で権限を壊せない
- [ ] 候補検索で古いユーザーも探せる

### 34-5. スロット

- [ ] 連続枠にreservation_group_idが付く
- [ ] 連続予約失敗時に更新済み枠を巻き戻す
- [ ] グループ内に別ユーザーが混在しない
- [ ] グループ内に別X IDが混在しない
- [ ] 中央部分解放でグループが分割される
- [ ] 隣接拡張でグループが結合される
- [ ] 上限超過が拒否される
- [ ] 時間重複が拒否される
- [ ] submitted枠が通常解放されない
- [ ] 表示はグループ単位
- [ ] 管理画面で個別枠も確認できる

### 34-6. 部番号

- [ ] イベントごとに部区切り時間を設定できる
- [ ] デフォルト30分
- [ ] 異常値は30分にフォールバック
- [ ] 時系列順で部番号が算出される
- [ ] 前方追加で部番号が再計算される
- [ ] 日付変更で部が分かれる
- [ ] 休憩閾値で部が分かれる
- [ ] 時間なし枠の扱いが定義されている
- [ ] 管理画面で算出根拠を確認できる

### 34-7. チャプターコメント

- [ ] 独立コメント欄がない
- [ ] video_commentsを新規利用していない
- [ ] コメントは必ず時刻に紐づく
- [ ] noteが本文として扱われる
- [ ] 返信・スレッドを作っていない
- [ ] 未承認X IDでは投稿不可
- [ ] BAN/TOS/CostGuardが効く
- [ ] public/unlisted以外には投稿不可
- [ ] YouTube側unlistedかつFlameNode内publicには投稿可
- [ ] private表示範囲が守られている
- [ ] marker_kindに依存した分岐を増やしていない

### 34-8. DB

- [ ] deprecated項目へ新規書き込みしていない
- [ ] video_commentsを使っていない
- [ ] outro_commentを新規利用していない
- [ ] primary_event_idとvideo_eventsが同期される
- [ ] system_settingsはglobal 1行
- [ ] 削除は論理削除・アーカイブ優先
- [ ] 監査ログが必要操作で残る

### 34-9. 公開API

- [ ] publicな作品だけ返す
- [ ] YouTube側unlistedでFlameNode内publicの作品は返す
- [ ] Discord IDを返していない
- [ ] emailやroleを返していない
- [ ] private/internal noteを返していない
- [ ] ページネーションがある
- [ ] limit上限がある
- [ ] キャッシュ戦略がある

### 34-10. health

- [ ] system_settingsがglobal 1行
- [ ] primary_event_idとvideo_eventsが同期
- [ ] 存在しないevent/video参照がない
- [ ] available slotにvideo_idがない
- [ ] submitted slotにvideo_idがある
- [ ] slot時間重複がない
- [ ] reservation_group_id内に別ユーザー混在がない
- [ ] public動画にyoutube_video_idがある
- [ ] voided動画が公開されない
- [ ] like_countと実数が大きくズレていない
- [ ] deprecated項目に新規データが増えていない

### 34-11. security

- [ ] accounts.access_tokenがnull
- [ ] rejected X IDがactiveでない
- [ ] 未承認X IDで投稿済み作品がない
- [ ] BANユーザーの書き込みがない
- [ ] TOS未同意ユーザーの書き込みがない
- [ ] 公開APIに内部情報が出ていない
- [ ] custom pageで危険HTMLが無効化される
- [ ] 管理操作がhistory_logsに残る

### 34-12. UI / UX

- [ ] 上部バーの情報量が整理されている
- [ ] X ID切替はアクセスしやすい
- [ ] テーマ切替はアクセスしやすい
- [ ] エントリーページは2択中心
- [ ] スロット確保ボタンが直接確保画面へ飛ぶ
- [ ] トップページに開催日時がある
- [ ] トップページに募集期間がある
- [ ] トップページに残り枠がある
- [ ] 文言が自然
- [ ] 行高が揃っている
- [ ] 狭い幅でも崩れない
- [ ] 編集可能箇所が分かる
- [ ] 編集不可箇所が分かる

### 34-13. マージ前最終確認

- [ ] typecheckが通る
- [ ] buildが通る
- [ ] DB migrationがローカルで通る
- [ ] 権限なしユーザーでAPI直叩きテスト済み
- [ ] X ID切替テスト済み
- [ ] 連続枠の部分解放テスト済み
- [ ] 連続枠の拡張テスト済み
- [ ] 部番号再計算テスト済み
- [ ] チャプターコメント投稿テスト済み
- [ ] 公開APIの漏洩チェック済み
- [ ] health/securityチェック済み

---

## 35. Claude Code メインエージェントへの最初の投入プロンプト

```text
あなたは FlameNode のメイン実装エージェントです。

まずルート直下の claude-code-subagent-assignment.md を読み、作業をフェーズ分割してください。このファイルは自己完結版であり、外部の4つの修正指示ファイルがなくても実装可能な正本です。

必要に応じて .claude/flamenode-subagent-routing.yaml も参照してください。ただし仕様詳細が衝突した場合は claude-code-subagent-assignment.md を優先してください。

モデル運用:
- 基本は Sonnet。
- 本当に簡単な探索・文言・要約のみ Haiku。
- ID/権限/DB/連続枠/security/公開API漏洩/仕様衝突は Opus またはメインエージェント判断。

最初に行うこと:
1. README と設計ディレクトリと現行コードを確認する。
2. 本ファイルに対応する現行コードの所在を調査する。
3. Phase 0 のファイル地図を作る。
4. いきなり実装せず、PR分割案を出す。
5. 最初の実装PRは ID/権限/共通書き込みガードから始める。

禁止:
- Haikuに仕様判断をさせない。
- フロントだけで権限修正を済ませない。
- deprecated項目を新規利用しない。
- 連続枠を表示だけでごまかさない。
```

---

## 36. 完結性セルフチェック

このファイル単体で以下を判断できる必要がある。

- [x] モデル選択ルール
- [x] Haiku/Sonnet/Opusの使い分け
- [x] 作業フェーズ
- [x] PR分割
- [x] Discord ID / X ID / Active X IDの責務
- [x] 共通書き込みガードの条件
- [x] 枠なし投稿・枠あり投稿の条件
- [x] 未承認X IDで許可される範囲
- [x] YouTube ID保存方針
- [x] YouTube側unlistedとFlameNode内部unlistedの違い
- [x] 連続枠・reservation_group_idの仕様
- [x] 部番号算出仕様
- [x] いいね・セーブ・ライブラリのX ID主体方針
- [x] チャプターコメント仕様
- [x] 公開APIの返却可/不可
- [x] health/securityチェック項目
- [x] UI/UX修正方針
- [x] 入力UI改善方針
- [x] DB整理・deprecated項目
- [x] 通知・運営フロー
- [x] 旧データ取り込み方針
- [x] 危険操作・監査ログ
- [x] 管理画面UX
- [x] テスト観点
- [x] マージ前チェックリスト

---

## 37. ひとことで覚える割当ルール

- 探すだけならHaiku。
- 普通に直すならSonnet。
- 壊したらやばいところはOpus。
- 権限・DB・security・連続枠はケチらない。
