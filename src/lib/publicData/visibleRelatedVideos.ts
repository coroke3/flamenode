/**
 * @deprecated Use relatedVideoProjection.resolveVisibleRelatedVideos.
 * Kept as a thin re-export for older imports/tests.
 */
export {
  RELATED_DEFAULT_LIMIT,
  RELATED_MAX_LIMIT,
  RELATED_MIN_LIMIT,
  resolveVisibleRelatedVideos,
} from "./relatedVideoProjection.ts";
