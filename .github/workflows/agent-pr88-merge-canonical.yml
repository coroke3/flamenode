name: Agent PR88 merge canonical

on:
  push:
    branches:
      - agent/ux-and-status-simplification
    paths:
      - .github/workflows/agent-pr88-merge-canonical.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  merge:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/ux-and-status-simplification
          fetch-depth: 0
      - name: Merge canonical branch with canonical changes taking precedence
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git fetch origin agent/db-canonical-migration-v2
          set +e
          git merge --no-commit --no-ff -X theirs origin/agent/db-canonical-migration-v2
          merge_status=$?
          set -e
          if [ "$merge_status" -ne 0 ]; then
            mapfile -t unresolved < <(git diff --name-only --diff-filter=U)
            if [ "${#unresolved[@]}" -gt 0 ]; then
              git checkout --theirs -- "${unresolved[@]}"
              git add -- "${unresolved[@]}"
            fi
          fi
          rm .github/workflows/agent-pr88-merge-canonical.yml
          git add -A
          git commit -m "DB正本移行ブランチを統合"
          git push origin HEAD:agent/ux-and-status-simplification
