# 枠確保の運営例外

Status: Active

- `event.slots` 権限を持つイベント運営スタッフまたは admin は、公開イベントの募集開始前にも枠を確保できる。
- 運営例外を明示した操作では、イベント設定の `max_slots_per_video` を超えて確保できる。ただし D1・監査の安全上限である20枠は超えない。
- 例外操作は通常ユーザーの受付期間・上限判定を迂回するため、枠確保画面に警告を表示し、Server Action でも `event.slots` を再認証する。
